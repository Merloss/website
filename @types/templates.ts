export interface PostData {
  title: string
  short_description?: string
  tags?: string[]
  image?: string
  published_at?: string
}

export interface DefaultData {
  title: string
  subtitle: string
  author: string
} 