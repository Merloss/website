export interface PostData {
    title: string
    description?: string
    readingTime?: number
    tags?: string[]
  }
  
  export interface DefaultData {
    title: string
    subtitle: string
    author: string
  }